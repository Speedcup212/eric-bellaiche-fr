[build]
  command = "npm run test:recueil-100 && npm run typecheck:portal && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
