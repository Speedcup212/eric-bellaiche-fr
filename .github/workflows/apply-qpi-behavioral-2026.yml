name: Apply QPI behavioral 2026 patch
on:
  push:
    paths:
      - '.github/workflows/apply-qpi-behavioral-2026.yml'
      - 'scripts/patch-qpi-behavioral-2026.cjs'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/patch-qpi-behavioral-2026.cjs
      - run: npm ci
      - run: npm run typecheck:portal
      - run: npm run build
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f scripts/patch-qpi-behavioral-2026.cjs .github/workflows/apply-qpi-behavioral-2026.yml
          git add -A
          git commit -m "Simplify investor profile questionnaire" || exit 0
          git push
