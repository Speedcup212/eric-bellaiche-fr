name: Dark regulatory cards
on:
  push:
    paths:
      - '.github/workflows/apply-summary-regulatory-cards-dark.yml'
      - 'scripts/patch-summary-regulatory-cards-dark.cjs'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/patch-summary-regulatory-cards-dark.cjs
      - run: npm ci
      - run: npm run typecheck:portal
      - run: npm run build
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/portal/CifDossierSummaryPage.tsx
          git commit -m "Use dark theme for regulatory document cards" || exit 0
          git push
