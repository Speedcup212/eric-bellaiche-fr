name: Apply documents missing contrast
on:
  push:
    paths:
      - '.github/workflows/apply-documents-missing-contrast.yml'
      - 'scripts/patch-documents-missing-contrast.cjs'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/patch-documents-missing-contrast.cjs
      - run: npm ci
      - run: npm run test:recueil-100
      - run: npm run typecheck:portal
      - run: npm run build
      - name: Commit change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/portal/ClientDocumentsPage.tsx
          git commit -m "Improve missing documents warning contrast" || exit 0
          git push
