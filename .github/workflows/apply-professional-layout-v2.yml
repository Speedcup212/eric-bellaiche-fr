name: Apply professional layout v2
on:
  push:
    paths:
      - '.github/workflows/apply-professional-layout-v2.yml'
      - 'scripts/patch-professional-layout-v2.cjs'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/patch-professional-layout-v2.cjs
      - run: npm ci
      - run: npm run typecheck:portal
      - run: npm run build
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f scripts/patch-professional-layout-v2.cjs .github/workflows/apply-professional-layout-v2.yml
          git add -A
          git commit -m "Compact professional layout columns" || exit 0
          git push