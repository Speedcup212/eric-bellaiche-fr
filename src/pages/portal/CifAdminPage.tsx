import { useEffect, useMemo, useState } from 'react';
import type { Session } from '@supabase/supabase-js';
import { BarChart3, CheckCircle2, ChevronRight, FileCheck2, FileText, LayoutDashboard, LogOut, Mail, Menu, Plus, Search, ShieldCheck, Sparkles, Trash2, UserRound, Users, X } from 'lucide-react';
import { supabase } from '../../lib/supabase';
import { messageFromError } from '../../portal/portalHelpers';

interface DossierRow { id:string; reference:string|null; libelle:string|null; recueil_status:string; statut:string; created_at?:string|null; }
interface InvestorInviteRow { investisseur_id:string; role_dossier:string; recueil_status?:string|null; qpi_status?:string|null; esg_status?:string|null; invite_sent_at?:string|null; invite_send_count?:number; investisseurs:{prenom:string;nom:string;email:string|null}|null; }
interface InviteStatusRow { dossier_id:string; investisseur_id:string; email:string|null; last_sent_at:string|null; send_count:number|string|null; }
interface InviteDraft { email:string; subject:string; body:string; link:string; sentAt?:string; dossierId:string; investisseurId:string; }
interface DossierView extends DossierRow { investors:InvestorInviteRow[]; }

function invitationBody(firstName:string, link:string):string { return `Bonjour ${firstName || ''},

Dans le cadre de notre accompagnement, je vous ai ouvert un espace client personnel et sécurisé sur eric-bellaiche.fr.

Cet espace vous permettra notamment de :
- déposer les documents nécessaires à l’étude de votre situation ;
- compléter et valider votre recueil d’informations patrimoniales ;
- remplir votre questionnaire de profil investisseur ;
- renseigner, si vous souhaitez exprimer des préférences de durabilité, le questionnaire correspondant ;
- consulter les documents réglementaires mis à votre disposition.

Si votre situation familiale est « Marié » ou « Pacsé », le conjoint sera rattaché automatiquement au dossier lors du recueil et disposera ensuite de son propre accès pour ses questionnaires personnels.

Pour activer votre accès, utilisez votre lien personnel :
${link}

Ce lien est personnel, valable 7 jours et ne doit pas être transféré.

Bien cordialement,

Eric Bellaiche
Conseiller en gestion de patrimoine — CIF
https://eric-bellaiche.fr`; }
const done=(value?:string|null)=>['completed','validated'].includes(value??'');
function progressOf(d:DossierView){if(!d.investors.length)return 0;const steps=d.investors.flatMap(i=>{const base=[done(i.recueil_status),done(i.qpi_status)];return i.esg_status==='not_applicable'?base:[...base,done(i.esg_status)];});return steps.length?Math.round((steps.filter(Boolean).length/steps.length)*100):0;}
function initials(d:DossierView){const n=d.investors.map(i=>i.investisseurs).filter(Boolean) as Array<{prenom:string;nom:string}>;if(!n.length)return'CL';return n.slice(0,2).map(x=>`${x.prenom?.[0]??''}${x.nom?.[0]??''}`).join('').slice(0,3).toUpperCase();}
function clientLabel(d:DossierView){const n=d.investors.map(i=>i.investisseurs).filter(Boolean) as Array<{prenom:string;nom:string}>;return n.length?n.map(x=>`${x.prenom} ${x.nom}`).join(' & '):(d.libelle||d.reference||'Dossier client');}
function formatSentAt(v?:string|null){return v?new Date(v).toLocaleString('fr-FR',{dateStyle:'short',timeStyle:'short'}):'';}
function StatusPill({ok,label}:{ok:boolean;label:string}){return <span className={`rounded-full border px-2.5 py-1 text-[11px] font-semibold ${ok?'border-emerald-200 bg-emerald-50 text-emerald-700':'border-blue-100 bg-blue-50 text-blue-700'}`}>{label}</span>}

export default function CifAdminPage(){
 const[session,setSession]=useState<Session|null>(null),[ready,setReady]=useState(false),[rows,setRows]=useState<DossierView[]>([]),[auth,setAuth]=useState({email:'eric.bellaiche@gmail.com',password:''}),[form,setForm]=useState({reference:'',libelle:'',p1:'',n1:'',e1:'',m1:''}),[errorMessage,setErrorMessage]=useState(''),[message,setMessage]=useState(''),[inviteDraft,setInviteDraft]=useState<InviteDraft|null>(null),[busy,setBusy]=useState(false),[sendingInviteDossierId,setSendingInviteDossierId]=useState<string|null>(null),[deletingId,setDeletingId]=useState<string|null>(null),[search,setSearch]=useState(''),[filter,setFilter]=useState<'all'|'progress'|'ready'>('all'),[showCreate,setShowCreate]=useState(false),[mobileMenu,setMobileMenu]=useState(false);
 const load=async()=>{const{data:current}=await supabase.from('app_users').select('role,actif').maybeSingle();if(!current?.actif||!['cif','admin'].includes(current.role)){setReady(false);return}setReady(true);const[d,l,s]=await Promise.all([supabase.from('dossiers').select('id,reference,libelle,recueil_status,statut,created_at').order('created_at',{ascending:false}),supabase.from('dossier_investisseurs').select('dossier_id,investisseur_id,role_dossier,recueil_status,qpi_status,esg_status,investisseurs(prenom,nom,email)').order('role_dossier'),supabase.rpc('get_client_invite_statuses')]);if(d.error)throw d.error;if(l.error)throw l.error;if(s.error)throw s.error;const sm=new Map(((s.data??[])as InviteStatusRow[]).map(x=>[`${x.dossier_id}:${x.investisseur_id}`,x]));const g=new Map<string,InvestorInviteRow[]>();((l.data??[])as unknown as Array<InvestorInviteRow&{dossier_id:string}>).forEach(x=>{const st=sm.get(`${x.dossier_id}:${x.investisseur_id}`);g.set(x.dossier_id,[...(g.get(x.dossier_id)??[]),{...x,invite_sent_at:st?.last_sent_at??null,invite_send_count:Number(st?.send_count??0)}])});setRows(((d.data??[])as DossierRow[]).map(x=>({...x,investors:g.get(x.id)??[]})))};
 useEffect(()=>{let a=true;void supabase.auth.getSession().then(({data})=>{if(!a)return;setSession(data.session);if(data.session)void load().catch(e=>setErrorMessage(messageFromError(e)))});const{data}=supabase.auth.onAuthStateChange((_e,n)=>{setSession(n);if(n)void load().catch(e=>setErrorMessage(messageFromError(e)));else setReady(false)});return()=>{a=false;data.subscription.unsubscribe()}},[]);
 const signIn=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setErrorMessage('');try{const{error}=await supabase.auth.signInWithPassword({email:auth.email.trim(),password:auth.password});if(error)throw error;await load()}catch(x){setErrorMessage(messageFromError(x))}finally{setBusy(false)}};
 const bootstrap=async()=>{try{const{error}=await supabase.rpc('bootstrap_cif');if(error)throw error;await load();setMessage('Accès CIF initialisé.')}catch(x){setErrorMessage(messageFromError(x))}};
 const signOut=async()=>{await supabase.auth.signOut();setReady(false);setRows([])};
 const create=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);try{const{data,error}=await supabase.rpc('create_client_dossier',{p_reference:form.reference||null,p_libelle:form.libelle||null,p_inv1_prenom:form.p1,p_inv1_nom:form.n1,p_inv1_email:form.e1.trim().toLowerCase(),p_inv1_mobile:form.m1||null,p_inv2_prenom:null,p_inv2_nom:null,p_inv2_email:null,p_inv2_mobile:null});if(error)throw error;setMessage(`Dossier créé : ${(data as {reference?:string}).reference??''}.`);setForm({reference:'',libelle:'',p1:'',n1:'',e1:'',m1:''});setShowCreate(false);await load()}catch(x){setErrorMessage(messageFromError(x))}finally{setBusy(false)}};
 const createInvite=async(dossierId:string,investor:InvestorInviteRow)=>{setErrorMessage('');setMessage('');setInviteDraft(null);setSendingInviteDossierId(dossierId);try{const email=investor.investisseurs?.email?.trim();if(!email)throw new Error('Email investisseur manquant.');if(!session?.access_token)throw new Error('Session cabinet expirée. Reconnecte-toi.');const{data:token,error}=await supabase.rpc('create_client_invite',{p_dossier_id:dossierId,p_investisseur_id:investor.investisseur_id,p_email:email,p_validity_days:7});if(error)throw error;const link=`https://eric-bellaiche.fr/espace-client/invitation?token=${encodeURIComponent(token as string)}`,subject='Votre espace client sécurisé — Cabinet Eric Bellaiche',body=invitationBody(investor.investisseurs?.prenom??'',link);const response=await fetch('/api/send-client-invite',{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${session.access_token}`},body:JSON.stringify({to:email,subject,body,dossierId,investisseurId:investor.investisseur_id})});const result=await response.json().catch(()=>({})) as {error?:string;sentAt?:string};if(!response.ok)throw new Error(result.error||'Échec de l’envoi Gmail.');setInviteDraft({email,subject,body,link,sentAt:result.sentAt,dossierId,investisseurId:investor.investisseur_id});setMessage(`Invitation envoyée automatiquement à ${email}.`);await load()}catch(x){setErrorMessage(messageFromError(x))}finally{setSendingInviteDossierId(null)}};
 const resendDraft=async()=>{if(!inviteDraft)return;const i=rows.find(r=>r.id===inviteDraft.dossierId)?.investors.find(x=>x.investisseur_id===inviteDraft.investisseurId);if(i)await createInvite(inviteDraft.dossierId,i)};
 const listInvestors=async(id:string)=>{const inv=rows.find(x=>x.id===id)?.investors??[];if(!inv.length){setErrorMessage('Aucun investisseur rattaché.');return}const primary=inv.find(x=>x.role_dossier==='investisseur_1')??inv[0];if(!primary.investisseurs?.email?.trim()){setErrorMessage('Email de l’identifiant 1 manquant.');return}await createInvite(id,primary)};
 const deleteDossier=async(row:DossierView)=>{const label=clientLabel(row);if(!window.confirm(`Supprimer définitivement le dossier « ${label} » ?\n\nCette action est irréversible.`))return;setDeletingId(row.id);setErrorMessage('');setMessage('');try{const{data,error}=await supabase.rpc('delete_client_dossier',{p_dossier_id:row.id});if(error)throw error;const result=data as {ok?:boolean;deleted_id?:string}|null;if(!result?.ok||result.deleted_id!==row.id)throw new Error('La suppression n’a pas été confirmée par le serveur.');await load();setMessage(`Dossier supprimé : ${label}.`)}catch(x){setErrorMessage(`Suppression impossible : ${messageFromError(x)}`)}finally{setDeletingId(null)}};
 const filteredRows=useMemo(()=>rows.filter(r=>{const p=progressOf(r),q=search.trim().toLowerCase();if(filter==='progress'&&(p===0||p===100))return false;if(filter==='ready'&&p!==100)return false;return !q||[r.reference,r.libelle,clientLabel(r),...r.investors.map(i=>i.investisseurs?.email)].filter(Boolean).some(v=>String(v).toLowerCase().includes(q))}),[rows,search,filter]);
 const totalClients=rows.reduce((s,r)=>s+Math.max(1,r.investors.length),0),readyCount=rows.filter(r=>progressOf(r)===100).length,activeCount=rows.filter(r=>{const p=progressOf(r);return p>0&&p<100}).length;
 if(!session)return <div className="min-h-screen bg-[#081426] px-4 py-12 flex items-center justify-center"><form onSubmit={signIn} className="w-full max-w-md rounded-[28px] bg-white p-8"><ShieldCheck/><h1 className="mt-4 text-3xl font-semibold">Cabinet Eric Bellaiche</h1><input type="email" required value={auth.email} onChange={e=>setAuth({...auth,email:e.target.value})} className="mt-6 w-full rounded-2xl border p-3"/><input type="password" required value={auth.password} onChange={e=>setAuth({...auth,password:e.target.value})} placeholder="Mot de passe" className="mt-3 w-full rounded-2xl border p-3"/><button disabled={busy} className="mt-4 w-full rounded-2xl bg-[#0F172A] p-3 text-white">Connexion</button>{errorMessage&&<p className="mt-3 text-red-700">{errorMessage}</p>}</form></div>;
 if(!ready)return <div className="p-10"><button onClick={()=>void bootstrap()}>Vérifier / initialiser mon accès CIF</button></div>;
 return <div className="min-h-screen bg-[#F6F9FD] text-[#0F172A]"><div className="flex min-h-screen"><aside className={`${mobileMenu?'fixed inset-y-0 left-0 z-50 flex':'hidden'} w-[280px] flex-col bg-[#0B172A] p-5 text-white lg:static lg:flex`}><p className="text-xl font-semibold">Eric Bellaiche</p><nav className="mt-8 space-y-2"><a href="/cabinet" className="flex gap-3 rounded-2xl bg-white/10 p-3"><LayoutDashboard/>Vue d’ensemble</a><a href="#dossiers" className="flex gap-3 p-3"><Users/>Dossiers clients</a></nav><button onClick={()=>void signOut()} className="mt-auto flex gap-2 p-3"><LogOut/>Déconnexion</button></aside><main className="min-w-0 flex-1"><header className="border-b bg-white p-4"><div className="mx-auto flex max-w-[1500px] justify-between"><button className="lg:hidden" onClick={()=>setMobileMenu(true)}><Menu/></button><h1 className="text-xl font-semibold">Gestion des dossiers</h1><button onClick={()=>setShowCreate(true)} className="rounded-2xl bg-[#0F172A] px-4 py-2 text-white"><Plus className="inline h-4 w-4"/> Nouveau dossier</button></div></header><div className="mx-auto max-w-[1500px] space-y-7 p-7"><section className="rounded-[30px] bg-[#0F172A] p-8 text-white"><h2 className="text-3xl font-semibold">Tous tes dossiers, documents et validations dans un seul espace.</h2><p className="mt-4">{rows.length} dossiers · {totalClients} clients · {readyCount} complets · {activeCount} en cours</p></section>{message&&<p className="rounded-2xl bg-emerald-50 p-4 text-emerald-700">{message}</p>}{errorMessage&&<p className="rounded-2xl bg-red-50 p-4 text-red-700">{errorMessage}</p>}{inviteDraft&&<section className="rounded-2xl bg-emerald-50 p-5"><div className="flex gap-2"><CheckCircle2/>Invitation envoyée à {inviteDraft.email}</div><p className="mt-2 text-sm">{formatSentAt(inviteDraft.sentAt)}</p><button onClick={()=>void resendDraft()} className="mt-3 rounded-xl bg-[#0F172A] px-4 py-2 text-white"><Mail className="inline h-4 w-4"/> Renvoyer l’invitation</button></section>}<section id="dossiers"><div className="mb-5 flex gap-3"><div className="relative flex-1"><Search className="absolute left-3 top-3 h-4 w-4"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Rechercher…" className="w-full rounded-2xl border py-2 pl-10"/></div>{(['all','progress','ready'] as const).map(f=><button key={f} onClick={()=>setFilter(f)} className="rounded-xl border px-3">{f==='all'?'Tous':f==='progress'?'En cours':'Complets'}</button>)}</div><div className="grid gap-4">{filteredRows.map(row=>{const p=progressOf(row),sent=row.investors.filter(i=>i.invite_sent_at),dates=sent.map(i=>i.invite_sent_at as string).sort(),lastSent=dates.length?dates[dates.length-1]:undefined,primary=row.investors.find(i=>i.role_dossier==='investisseur_1')??row.investors[0],primaryInvited=Boolean(primary?.invite_sent_at);return <article key={row.id} className="rounded-[26px] border bg-white p-6"><div className="flex flex-col gap-5 xl:flex-row xl:items-center"><div className="flex flex-1 gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-blue-50 font-bold text-blue-600">{initials(row)}</div><div><h3 className="text-lg font-semibold">{clientLabel(row)}</h3><div className="mt-3 flex flex-wrap gap-2"><StatusPill ok={row.investors.length>0&&row.investors.every(i=>done(i.recueil_status))} label="Recueil"/><StatusPill ok={row.investors.length>0&&row.investors.every(i=>done(i.qpi_status))} label="Profil"/><StatusPill ok={row.investors.length>0&&row.investors.every(i=>done(i.esg_status))} label="ESG"/>{lastSent&&<span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[11px] font-semibold text-emerald-700">Invitation envoyée · {formatSentAt(lastSent)}</span>}</div></div></div><div className="w-full xl:max-w-[300px]"><p className="text-xs">Avancement {p}%</p><div className="mt-2 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-500" style={{width:`${p}%`}}/></div></div><div className="flex flex-wrap gap-2"><a href={`/cabinet/synthese?dossier=${encodeURIComponent(row.id)}`} className="rounded-xl bg-[#0F172A] px-3 py-2 text-sm text-white">Ouvrir <ChevronRight className="inline h-4 w-4"/></a><a href={`/cabinet/audit?dossier=${encodeURIComponent(row.id)}`} className="rounded-xl border px-3 py-2 text-sm"><BarChart3 className="inline h-4 w-4"/> Audit</a><a href={`/cabinet/adequation?dossier=${encodeURIComponent(row.id)}`} className="rounded-xl border px-3 py-2 text-sm"><FileCheck2 className="inline h-4 w-4"/> Adéquation</a><button disabled={sendingInviteDossierId===row.id} onClick={()=>void listInvestors(row.id)} className={`rounded-xl border px-3 py-2 text-sm ${primaryInvited?'bg-emerald-50 text-emerald-700':''}`}><Mail className="inline h-4 w-4"/> {sendingInviteDossierId===row.id?'Envoi…':primaryInvited?'Renvoyer':'Inviter'}</button><button disabled={deletingId===row.id} onClick={()=>void deleteDossier(row)} className="rounded-xl border border-red-200 px-3 py-2 text-sm text-red-700 disabled:cursor-not-allowed disabled:opacity-50"><Trash2 className="inline h-4 w-4"/> {deletingId===row.id?'Suppression…':'Supprimer'}</button></div></div></article>})}{!filteredRows.length&&<div className="rounded-2xl border bg-white p-10 text-center"><UserRound className="mx-auto"/>Aucun dossier trouvé</div>}</div></section><section className="rounded-[30px] border bg-white p-6"><h2 className="text-2xl font-semibold">Intégrations</h2><div className="mt-4 flex gap-3"><FileText/> Gmail / envoi + traçabilité <Sparkles/></div></section></div></main></div>{mobileMenu&&<button className="fixed inset-0 z-40 lg:hidden" onClick={()=>setMobileMenu(false)}><X/></button>}{showCreate&&<div className="fixed inset-0 z-[60] flex items-center justify-center bg-black/50 p-4"><form onSubmit={create} className="w-full max-w-2xl rounded-[30px] bg-white p-8"><h2 className="text-2xl font-semibold">Créer un client</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{(['reference','libelle','p1','n1','e1','m1'] as const).map(k=><input key={k} required={['p1','n1','e1'].includes(k)} type={k==='e1'?'email':'text'} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})} placeholder={{reference:'Référence (facultatif)',libelle:'Libellé du dossier',p1:'Prénom',n1:'Nom',e1:'Email personnel',m1:'Mobile'}[k]} className="rounded-2xl border p-3"/>)}</div><div className="mt-5 flex justify-end gap-3"><button type="button" onClick={()=>setShowCreate(false)}>Annuler</button><button disabled={busy} className="rounded-2xl bg-[#0F172A] px-5 py-3 text-white">Créer le dossier</button></div></form></div>}</div>;
}
